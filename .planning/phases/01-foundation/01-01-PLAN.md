---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css]
---

<objective>
Create Next.js project with App Router and Tailwind CSS configured.

Purpose: Establish the foundation for a mobile-first PWA with modern React patterns.
Output: Working Next.js app with Tailwind styling, ready for database integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with App Router</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` to scaffold Next.js in current directory.

    Accept defaults for all prompts (use --yes or answer interactively).

    This creates:
    - App Router structure in src/app/
    - TypeScript configuration
    - Tailwind CSS preconfigured
    - ESLint for code quality
    - npm as package manager
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>Next.js app created with App Router, TypeScript, and Tailwind. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Tailwind and create placeholder home page</name>
  <files>src/app/page.tsx, src/app/globals.css</files>
  <action>
    Replace default page.tsx content with a simple placeholder that verifies Tailwind is working:

    - Mobile-first layout with centered content
    - Use Tailwind utility classes (text-2xl, font-bold, bg-blue-500, etc.)
    - Display "Fuel Tracker" heading and brief "Coming soon" message
    - Ensure globals.css has Tailwind directives (@tailwind base/components/utilities)

    Keep it minimal - just enough to prove Tailwind works.
  </action>
  <verify>npm run dev starts successfully, visiting localhost:3000 shows styled page with Tailwind classes applied</verify>
  <done>Home page displays with Tailwind styling. Dev server runs without errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts dev server
- [ ] localhost:3000 shows styled placeholder page
- [ ] No TypeScript errors
</verification>

<success_criteria>

- Next.js 14+ with App Router scaffolded
- Tailwind CSS configured and working
- TypeScript compilation passes
- Dev server runs successfully
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
